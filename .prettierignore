# oxfmt reads this by default (alongside .gitignore) to decide what to
# skip. Generated files are checked in but must not be formatted — the
# generator rewrites them unformatted on every build, which would
# otherwise break `bun run fmt:check`.

# TanStack Router route tree (regenerated by `tsr generate` / the vite
# plugin; its own header says to exclude it from linters/formatters).
src/routeTree.gen.ts
